For correct operation, add the .env file to the root of the project. 
The file must contain the following lines for your database:

DB_HOST=
DB_PORT=
DB_USER=
DB_PASS=

You will also need a GEMINI API KEY, which must be registered in your system's env.
For example: GEMINI_API_KEY=JFIDFIU238_2831DFUD